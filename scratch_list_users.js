const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const users = await prisma.user.findMany({ where: { role: 'AFFILIATE' }, take: 5 }); console.log(JSON.stringify(users, null, 2)); process.exit(0); } main();
